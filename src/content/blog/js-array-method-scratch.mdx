---
# ---- REQUIRED ----
title: "JS Array Method Scratch"
description: "Hand-implementing JavaScript array core methods: forEach, map, filter, reduce"
pubDate: "Jun 09 2026"
tags: ["JavaScript", "Array", "Implementation"]
# ---- OPTIONAL ----
published: true
# hidden: false
# shortDescription: ""
# updatedDate: ""
# heroImage: "/src/assets/posts/image.jpg"
# hideHero: false
# noImage: false
# customOGImage: "/src/assets/posts/image.jpg"
---

## forEach

**Standard Definition:** `arr.forEach(callback(element, index, array), thisArg)`

```javascript
const myForEach = (arr, callback) => {
  for (let i = 0; i <= arr.length - 1; i++) {
    callback(arr[i], i, arr);
  }
};
```

## map

**Standard Definition:** `arr.map(callback(element, index, array), thisArg)`

```javascript
const myMap = (arr, callback) => {
  let res = new Array(arr.length);
  for (let i = 0; i <= arr.length - 1; i++) {
    res[i] = callback(arr[i], i, arr);
  }
  return res;
};
```

## filter

**Standard Definition:** `arr.filter(callback(element, index, array), thisArg)`

```javascript
const myFilter = (arr, callback) => {
  let res = [];
  for (let i = 0; i <= arr.length - 1; i++) {
    let flag = callback(arr[i], i, arr);
    if (flag) res.push(arr[i]);
  }
  return res;
};
```

## reduce

**Standard Definition:** `arr.reduce(callback(accumulator, currentValue, index, array), initialValue)`

```javascript
const myReduce = (arr, callback, default_value) => {
  let acc, startIndex;
  if (default_value != undefined) {
    acc = default_value;
    startIndex = 0;
  } else {
    acc = arr[0];
    startIndex = 1;
  }

  for (let i = startIndex; i <= arr.length - 1; i++) {
    acc = callback(acc, arr[i], i, arr);
  }
  return acc;
};
```
